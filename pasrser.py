# Parser tools for taking in .in files and convering into python data structures

def parse_input(filename):
	f = open(filename, '+r')
	# Read the file, put into 2D array
	# Generate graph if we choose to, may be easier to work with graph rep for things like djikstras or BFS/DFS

